import { Text } from "react-native";
import { SafeAreaView } from 'react-native-safe-area-context';

const HomeScreen: React.FC = () => {
    return (
        <SafeAreaView>
            <Text>Welcome to Cinemax!</Text>
        </SafeAreaView>
    );
};

export default HomeScreen;